config-version: 1
server-id: CHANGE_ME

shutdown-timeout:
  seconds: 15

executor:
  threads: 2
  thread-name-prefix: Wayfarer-Core

audit:
  enabled: true

health:
  player-details: false

mariadb:
  enabled: false
  jdbc-url-env: WAYFARER_DB_URL
  username-env: WAYFARER_DB_USERNAME
  password-env: WAYFARER_DB_PASSWORD
  maximum-pool-size: 8
  minimum-idle: 1
  connection-timeout-ms: 5000

redis:
  enabled: false
  uri-env: WAYFARER_REDIS_URI
  connect-timeout-ms: 3000

migration:
  enabled: false
  locations:
    - db/migration/core

waymark:
  enabled: false
  expected-provider: RedisEconomy
  operation-timeout-ms: 5000
