# V0.0.1-alpha.4 Test Plan — Waymark Transactions

- Release scope: `core`
- Requirement IDs: CORE-007, CORE-008, TEST-004
- Test environment: isolated MariaDB/Redis and supported Waymark provider boundary

## Preconditions

Provider-independent authority is ADR 0006. RedisEconomy/Vault concrete invocation remains blocked
because a fixed safe thread/timeout/reference contract was not established. Execute the engine,
repository, reconcile, command, and fixture suite without a concrete adapter.

## Automated

- Cover every transaction state and allowed transition, provider reference, timeout, retry,
  insufficient funds, outage, crash windows, UNKNOWN, automatic/manual reconcile, and audit.
- Reuse an idempotency key and prove exact equality includes payload, subject type, and nullable
  actor; prove no duplicate debit or refund under concurrency/restart.
- Inject crashes after every durable debit/refund boundary and prove recovery resolves persisted
  operation IDs without replacing them or repeating an effect.
- Prove debit/refund operation IDs and references remain separate in MariaDB, public inspection,
  and append-only history.
- Fail transition audit mirrors and prove durable state/history progress, observable sanitized
  degradation, and zero duplicate effects.
- Race execute/recovery/manual reconcile workers and prove one winning external-effect path.
- Prove external-provider operations are not claimed as unconditional exactly-once.
- Verify inspect/reconcile permissions, confirmation, redaction, and console policy.

## Runtime

Verify balance, debit, refund, failure paths, duplicate requests, UNKNOWN/reconcile, restart
recovery, and provider outage using isolated accounts and explicit evidence.

## Exit

No duplicate fixture economic effect exists. Provider-independent work may pass while concrete
provider completion remains explicitly `BLOCKED`.
