# V0.0.1-beta.1 Test Plan — Feature Complete

- Release scope: `core`
- Requirement IDs: all applicable V0.0.1 Core and automated-test requirements

## Automated

- Run the full unit, MariaDB, Redis, migration, concurrency, idempotency, timeout, restart,
  config, redaction, threading, callback, and duplicate-effect suites.
- Run `check` and `assemble` with and without configuration cache.
- Inspect dependency direction, public API compatibility/class identity, reproducibility,
  relocation/bundling, licenses/notices, and JAR contents.
- Prove only `Wayfarer_Core-V0.0.1-beta.1.jar` is selected for release scope `core`.

## Documentation

Complete sanitized config, commands/permissions, dependencies, migrations, known limitations,
license/third-party notices, traceability, and all phase result records.

## Exit

No major implementation gap remains; every failed/skipped/N/A result has a reason and next action.
