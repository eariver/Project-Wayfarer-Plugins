# V0.0.2 Candidate-8 Pre-remediation Defect Sweep

Revision: A  
Recorded: 2026-08-05 JST  
Status: `PREPARED_WAITING_FOR_OWNER_START`

## 1. Purpose

Candidate-8 Server-side Runtime Preflight passed and `C8-MAIN-01` passed. Formal Main Client
Acceptance stopped at `C8-MAIN-02` after the Growth Tool GUI did not open and representative
progress remained zero. Independent source review also identified a confirmed GUI-entry defect and
additional questions around normal durability consumption and `wayfarer.main.use` enforcement.

Do not begin Candidate-9 implementation yet. First perform this bounded pre-remediation defect sweep
so all closely related Main foundation defects can be fixed together in one Candidate-9 Product
change where practical.

Luna is the executor and evidence recorder. Luna must not redesign the Product, propose unrelated
features, expand the test matrix, or implement any correction during this sweep.

## 2. Fixed identities and current state

```text
Candidate label:
  V0.0.2-Client-Candidate-8

Candidate-8 Product HEAD:
  698c387dfe2e86de8e48ea59a80f35f14c728e2a

Prepared instruction commit parent:
  368059308d6c096c38922a147f2c91e18a3f1fe8

C8-MAIN-01:
  PASS

C8-MAIN-02:
  FAIL / FORMAL MAIN ACCEPTANCE PAUSED

Candidate-9 implementation:
  NOT STARTED
```

Preserve the exact Candidate-8 Main and Frontier JARs, dependency JARs, disposable database, Redis
namespace, runtime directories, worksheets, logs, screenshots, and `DEFECT_REGISTER.md` from the
current attempt. Do not overwrite or rename the current evidence tree while this sweep is active.

## 3. Absolute boundaries

During this sweep:

- do not modify Product source, tests, build logic, migrations, schemas, or workflow files;
- do not rebuild or replace Candidate-8 JARs;
- do not edit Main or Frontier configuration;
- do not edit Item NBT/PDC;
- do not edit Wayfarer database rows or Redis keys;
- do not use direct RedisEconomy internal-key manipulation;
- do not perform paid Reissue, Repair payment, Branch change, admin Reissue/Revoke, or reconcile;
- do not perform Main death, non-owner, restart-persistence, or later formal Main scenarios;
- do not grant additional permissions except the one temporary deny/restore operation explicitly
  authorized in section 6;
- do not commit or push evidence or status changes;
- do not start Candidate-9 implementation.

Stop the entire sweep immediately on duplicate durable delivery, authority corruption, unintended
Waymark debit, data loss, server crash, security bypass outside the stated permission probe, or
cross-backend contamination.

## 4. Formal acceptance state

Formal Main Client Acceptance remains paused at `C8-MAIN-02`. Diagnostic actions in this document
must not be marked as `C8-MAIN-03` or later PASS/FAIL results.

Frontier Client Acceptance may continue after section 7 only when:

- both backends remain enabled and healthy;
- the shared disposable MariaDB and Redis namespace remain consistent;
- no Main diagnostic action produced authority corruption, duplicate Item state, or cross-backend
  contamination;
- the Frontier JAR and Fixture bytes remain unchanged.

Any valid Frontier result belongs to the unchanged Candidate-8 Frontier artifact. Preserve it for
possible carry-forward when Candidate-9 changes Main only. Do not promise carry-forward until the
final Candidate-9 change scope is known.

## 5. Read-only source and runtime review

Before asking the Owner for another Gameplay action, review and record the current Candidate-8
behavior of only these Main foundation paths:

1. `PlayerInteractEvent` GUI entry, including cancelled `RIGHT_CLICK_AIR` delivery;
2. `BlockBreakEvent` authorization guard;
3. representative Progress update;
4. `PlayerItemDamageEvent` normal damage, evolution restoration, repair exclusion, authority
   rejection, and terminal Broken conversion;
5. Held Authorization invalidation and deferred reauthorization after held-slot, inventory, drop,
   pickup, swap-hand, and respawn transitions;
6. `wayfarer.main.use` enforcement on Player Gameplay entry points.

Primary source boundary:

```text
plugins/wayfarer-main/src/main/java/io/github/eariver/wayfarer/main/gameplay/
MainGameplayRuntime.java

plugins/wayfarer-main/src/main/java/io/github/eariver/wayfarer/main/gameplay/
HeldGrowthToolAuthorizer.java

plugins/wayfarer-main/src/main/java/io/github/eariver/wayfarer/main/application/
MainCommandPermissionPolicy.java

plugins/wayfarer-main/src/main/resources/plugin.yml
```

Review only the directly called helper classes necessary to understand these paths. Do not broaden
into Frontier, delivery concurrency, migration, pricing, transaction recovery, build, or release
architecture.

For each suspected defect, record:

- exact method and condition;
- expected contract;
- observed or logically implied failure;
- whether Client reproduction is still needed;
- proposed classification only, not a code change.

Known starting observation:

```text
GUI entry:
  RIGHT_CLICK_AIR is required, while the current listener ignores cancelled events.
  Treat this as a confirmed Product defect unless the current checked-out Product bytes differ.
```

## 6. One bounded Main runtime diagnostic

Use the existing P1 and current Candidate-8 Main runtime. Ask the Owner for one action at a time.
Do not repeat an unambiguous result.

### 6.1 Preconditions

Read and record without mutation:

```text
backend:
  Main

world:
  resource

game mode:
  SURVIVAL

Main runtime:
  ENABLED

permission:
  wayfarer.main.use = true

authority:
  ACTIVE

delivery:
  DELIVERED

epoch:
  current expected Candidate-8 epoch

physical item:
  exactly one canonical Wayfarer Growth Pickaxe in Main Hand

progress:
  value before action

item damage:
  value before action
```

When any precondition is false, do not repair it silently. Record
`ENVIRONMENT_OR_SETUP_MISMATCH` and stop the Main runtime diagnostic.

### 6.2 Representative break

Owner action:

```text
In exact world resource, while in SURVIVAL and holding the canonical Growth Pickaxe in Main Hand,
break exactly one ordinary STONE block.
```

Record:

- whether the block actually disappeared;
- whether its normal drop appeared;
- Progress before and after;
- Item damage before and after;
- current authority, item instance, Epoch, and status after the action;
- relevant sanitized console output;
- any evidence that `BlockBreakEvent` or `PlayerItemDamageEvent` was cancelled;
- whether an evolution threshold was crossed.

At the initial threshold state, one ordinary STONE must not be treated as an evolution transition.
When the actual pre-action Progress is near a threshold, choose another eligible ordinary block or
record `THRESHOLD_PROXIMITY_BLOCKED`; do not mutate Progress to avoid the threshold.

Classify the unambiguous outcome:

```text
Block does not break:
  SUSPECT_HELD_AUTHORIZATION_OR_BREAK_GUARD_DEFECT

Block breaks, Progress does not increase:
  SUSPECT_PROGRESS_ENTRY_DEFECT

Block breaks, Progress increases, normal Item damage does not increase:
  SUSPECT_NORMAL_DURABILITY_DEFECT

Block breaks, Progress and normal Item damage both increase exactly once:
  FOUNDATION_BREAK_PATH_PASS

Unexpected identity, Epoch, status, duplicate, or delivery change:
  AUTHORITY_CORRUPTION / STOP ENTIRE SWEEP
```

Do not run a second block break unless the first result is genuinely ambiguous because the server
or Client did not show whether the action completed.

### 6.3 Temporary permission-deny probe

Run this only when section 6.2 leaves authority and runtime state trustworthy.

Using LuckPerms, temporarily set only `wayfarer.main.use=false` for P1 in the disposable test
context. Record the exact temporary command and prior effective value.

Owner action:

```text
With the canonical Growth Pickaxe in Main Hand, attempt to break exactly one ordinary STONE block
in resource.
```

Pass criterion for the permission contract:

- Player-use behavior is rejected safely;
- no Progress is added;
- no authority, identity, delivery, Epoch, or Waymark state changes.

Record whether the physical block was broken. The expected denial presentation may be silent; do
not add a new presentation requirement during this sweep.

Immediately restore the exact prior effective permission and verify
`wayfarer.main.use=true`. Do not test additional permission nodes.

Classify:

```text
Use is safely denied:
  PERMISSION_GAMEPLAY_GATE_PASS

Use remains available or Progress increases:
  CONFIRMED_PERMISSION_GAMEPLAY_GATE_DEFECT

Permission cannot be changed/restored safely:
  ENVIRONMENT_OR_OPERATOR_BLOCK / STOP MAIN DIAGNOSTIC
```

The known GUI defect means this permission probe uses representative Block Break only. Do not use
GUI non-opening as permission evidence.

## 7. Main sweep stop and report

After sections 5 and 6, stop Main diagnostic actions. Do not proceed to Drop/Pickup, Chest, Death,
Reissue, Repair, Branch, non-owner, or Main restart tests.

Update only the local ignored evidence:

```text
results/DEFECT_REGISTER.md
results/CANDIDATE_8_PRE_REMEDIATION_SWEEP_RESULT.md
worksheet/C8_MAIN_02.md
```

Preserve the original C8-MAIN-02 failure entry. Add a clearly separated diagnostic appendix rather
than rewriting the original observation.

The result must list each finding as one of:

```text
CONFIRMED_PRODUCT_DEFECT
SUSPECT_PRODUCT_DEFECT_NEEDS_FOCUSED_AUTOMATED_TEST
ENVIRONMENT_OR_SETUP_MISMATCH
OPERATOR_RESOURCE_BLOCK
NOT_REPRODUCED
FOUNDATION_PATH_PASS
```

For each finding include exact source location or runtime action, expected result, actual result,
impact, and the smallest regression-test obligation. Do not propose complete implementation code.

Report to the Owner and stop before Candidate-9 implementation.

## 8. Frontier continuation

When section 7 concludes without a global stop condition and the runtime remains trustworthy, resume
Frontier Client Acceptance from the first unexecuted `C8-FRONT-*` scenario in:

```text
docs/testing/plans/V0.0.2-candidate-8-runtime-and-client-execution.md
```

Use the existing one-action-at-a-time Owner interaction, bounded repetition, defect policy, and
local evidence requirements. Do not rerun already unambiguously passed Frontier scenarios.

A Frontier Product defect stops the affected Frontier scenario. Continue unaffected Frontier
scenarios only when state remains trustworthy. Do not alter the approved Fixture or balance during
the test.

At the end, report Frontier results separately from the failed Candidate-8 Main result. Do not claim
full Candidate-8 Client Acceptance PASS.

## 9. Required final report

Luna's final response for this instruction must use this structure:

```text
CANDIDATE-8 MAIN FORMAL ACCEPTANCE:
  PAUSED AT C8-MAIN-02

PRE-REMEDIATION MAIN DEFECT SWEEP:
  COMPLETE / BLOCKED / STOPPED

CONFIRMED PRODUCT DEFECTS:
  <concise list>

SUSPECT DEFECTS REQUIRING AUTOMATED REPRODUCTION:
  <concise list or NONE>

FOUNDATION BREAK PATH:
  PASS / FAIL / BLOCKED

WAYFARER.MAIN.USE GAMEPLAY GATE:
  PASS / FAIL / BLOCKED

FRONTIER CLIENT ACCEPTANCE:
  <completed scenario range and result summary>

PRODUCT SOURCE / CONFIG / FIXTURE:
  UNCHANGED

CANDIDATE-9 IMPLEMENTATION:
  NOT STARTED / WAITING FOR CHATGPT REMEDIATION HANDOFF

PR #14:
  OPEN / DRAFT / UNMERGED
```

Do not commit, push, implement, rebuild, merge, tag, publish, change PR readiness, or infer
`requirements_cleared` under this instruction.