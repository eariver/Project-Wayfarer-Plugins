# V0.0.2 Bounded Client Acceptance Plan

Status: `CLIENT_TEST_REQUIRED`; Client Acceptance is `NOT STARTED`. Client Test Candidate
`V0.0.2-Client-Candidate-1` is fixed, and its first-test LeafGrapple baseline is approved by the
external Frontier authority. Run only in a separate disposable Paper environment after the
Plugin-side handoff receives independent review. Never use Project Runtime, Project worlds,
data, secrets, or players.

## Preconditions and evidence

- Use product source commit `90c3f5fe0f02fe297bd6d12f596ce6c9bac27cce`, exact published Core
  V0.0.1 reuse, and the exact Main/Frontier artifact hashes recorded in the candidate handoff.
- Candidate label: `V0.0.2-Client-Candidate-1`; local staging path is
  `.ai-work/luna-gpt-5.6-v002/candidate/V0.0.2-Client-Candidate-1/`.
- Record pinned Paper/external-plugin versions and hashes plus sanitized configuration.
- Use task-only MariaDB/Redis and disposable worlds.
- Capture bounded commands/actions, expected and actual results, screenshots where visual, and
  redacted server evidence. Do not record secrets, raw Player IDs, raw exceptions, or provider
  objects.

## Frontier-approved first-test Fixture

The external authority is Project Issue `eariver/Project_Wayfarer#4`, approval comment
https://github.com/eariver/Project_Wayfarer/issues/4#issuecomment-5155937809 (comment ID
`5155937809`). Its exact decision is:

```text
Concrete Fixture:
  ACKNOWLEDGED

First Client Test baseline:
  APPROVED

Candidate:
  V0.0.2-Client-Candidate-1

Use:
  Client Test only

Pre-test balance changes:
  NONE

Production promotion:
  DECIDE_AFTER_CLIENT_TEST
```

Use this immutable Fixture without edits:

```text
Fixture path:
  docs/testing/fixtures/V0.0.2/leafgrapple-wayfarer-test-only.yml

Fixture commit:
  521a41bbcc4d4e0e58111deeb663f52bf1c6e1af

Fixture SHA-256:
  ed210f8e56db26315f91fecb9e1d35d686c8fe647480498b7588467a6fa2448a

LeafGrapple artifact:
  LeafGrapple.jar
  plugin version 1.0.2
  embedded Maven version 1.0.2-SNAPSHOT
  size 56175 bytes
  SHA-256 FFE4B3305BB48737E1B6C373698FEFE7121B879FD5B9399F930F5023B5F78833
```

Approved first-test configuration values are:

```text
display-name: Wood Grapple
item-material: PAPER
item-model: leafgrapple:wood_grapple
display-item-material: PAPER
display-item-model: leafgrapple:wood_grapple_head

max-distance: 16.0
max-pull-distance: 32.0
launch-speed: 1.2
pull-acceleration: 0.05
max-pull-speed: 0.85
cooldown-ticks: 20

durability.enabled: false
durability.max: 32
durability.cost-on-finish: 1

display.glowing: true
display.scale-x: 0.35
display.scale-y: 0.35
display.scale-z: 0.35
display.offset-x: 0.0
display.offset-y: -0.1
display.offset-z: 0.0
display.rotation-axis-x: 0.0
display.rotation-axis-y: 1.0
display.rotation-axis-z: 0.0
display.rotation-angle-degrees: 0.0

lore.show-technical-info: false

entity-hook.enabled: false
entity-hook.target.players: false
entity-hook.target.mobs: false
entity-hook.target.animals: false
entity-hook.target.monsters: false
```

The complete Entity Hook subtree remains copied from `hooks.wood` even though it is disabled.
Do not remove, normalize, rebalance, or add world restrictions to the Fixture.

The LeafGrapple Tier Fixture owns public tier, item/model/display, range, pull, movement,
cooldown, durability-disabled, and entity-hook-disabled behavior. The Wayfarer Runtime Guard is
separate: it accepts only exact case-sensitive `frontier_iris`, rejects Themes outside that name,
stays enabled while `frontier_iris` is absent/unloaded, never creates the world, and leaves world
creation/loading to Multiverse. Do not attribute this boundary to LeafGrapple.

## Main representative checks

1. Confirm one canonical Growth Tool, owner use, non-owner/old-item rejection, reconnect, and no
   duplicate delivery.
2. Mine representative eligible and ineligible blocks; observe progress, evolution, and
   saturation-safe behavior without claiming exhaustive balance.
3. Observe broken state and repair preview/confirm/cancel, double-click, disconnect, and
   reconnect.
4. Verify death removes the managed tool without automatic return; exercise the paid Player
   reissue quote/confirm, old-item rejection, Pending Delivery, and free retry.
5. Verify Main V004/reissue recovery routes, sanitized output, and permission allow/deny behavior.

## Frontier representative checks

1. Enter exact `frontier_iris`; reject similar/nether/end worlds; confirm initial loadout,
   reconnect non-duplication, full-inventory Pending Delivery, and stale-identity guards.
2. With the approved first-test Fixture, observe canonical hook motion, durability-disabled
   behavior, and entity/player/mob/animal/monster-hooking rejection.
3. Verify current English Navigation presentation and implemented actions; deferred Waystone is
   absent/non-actionable.
4. Place/use Launchpad and verify current-view direction, Sneak, cooldown, successful uses,
   auto-Elytra, restart/reconcile, and supported protection behavior.
5. Exercise Frontier permanent-item death redelivery, Safe Entry/respawn reconciliation, and
   free retry. Confirm Launchpad/Rocket are not treated as permanent free-redelivery items.
6. Verify permission leaves through LuckPerms, no OP dependency, sanitized output, exact portal
   boundary, and End Gateway as an observation only.

## Mandatory LeafGrapple scenarios

The first Client Test must cover all of the following without expanding into an unbounded stress
matrix:

1. short-range Block Hook;
2. horizontal long-range Hook;
3. upward Hook to cliff, mountain, tree, or structure;
4. cave/ceiling/narrow-space Hook;
5. Hook-to-Elytra transition;
6. Hook use after Launchpad;
7. cooldown-boundary repeated input;
8. Player/Mob/Animal/Monster Hook rejection;
9. Runtime Guard rejection outside exact `frontier_iris`;
10. fail-closed behavior for missing LeafGrapple, wrong version, missing tier, and unsafe
    configuration;
11. reissued Hook remains a canonical LeafGrapple item;
12. Resource Pack display checks for held Hook and launched Hook Head.

## Risk-based repetitions

Apply these counts to motion and interaction scenarios:

```text
Basic motion scenarios:
  normally 3 attempts
  stop after 3 consecutive unambiguous successes

Cooldown boundary:
  at least 5 consecutive inputs

Entity Hook rejection:
  1–2 representative attempts per configured target category

Anomaly, instability, or unclear reproducibility:
  hold conditions constant
  expand the affected scenario to at least 10 attempts
```

Artifact identity, configuration-presence, version/tier mismatch, unsafe-configuration, and
other fail-closed variants need one unambiguous result per distinct state. Repeat only when the
outcome is ambiguous or a bounded defect reproduction is required. A major failure observed once
is not diluted by later successes; preserve the reproduction evidence and classify its impact.

## Required boundary observations

### Range

- success within 16 blocks;
- boundary behavior near 16 blocks;
- safe rejection beyond 16 blocks;
- detach/break behavior when Player–Anchor distance approaches 32 blocks.

### Motion

- horizontal distance and controllability;
- vertical height gained;
- transition from `launch-speed=1.2` to pull;
- acceleration feel from `pull-acceleration=0.05`;
- speed cap behavior at `max-pull-speed=0.85`;
- input rejection during the 20-tick cooldown;
- successful reuse after cooldown.

### Elytra and Launchpad

- altitude/velocity/instability when deploying Elytra after Hook motion;
- velocity composition immediately after Launchpad;
- evidence when Hook is too weak to support Elytra transition;
- evidence when Hook over-shortcuts intended terrain.

### Display

Under the intended Resource Pack condition:

- record the Resource Pack identity/version and hash when available;
- held Hook model renders;
- launched Hook Head model renders;
- glowing/scale/offset do not obstruct operation.

If the intended Resource Pack is unavailable, mark the display checks `BLOCKED` and do not claim
complete Client Acceptance. Do not classify a missing-pack display result as movement balance.
Record display defects separately from movement balance.

## Stop and defect policy

```text
Stop once a representative result is unambiguous.
Do not expand into stress, crash matrices, or unrelated features.
A defect gets one bounded reproduction first.
```

Only the affected scenario expands when evidence is unstable or a defect requires confirmation.

## Required Frontier result record

The later client result/handoff must record:

- exact LeafGrapple JAR filename, version, and SHA-256;
- exact Fixture path, commit, SHA-256, and configuration values actually used;
- Resource Pack identity/version/hash and held/launched model observations;
- durability-disabled and entity/player/mob/animal/monster-hooking-disabled results;
- Wayfarer capability detection and exact `frontier_iris` Runtime Guard result;
- canonical item creation, reissue, and delivery result;
- identity, death, and redelivery result;
- bounded range, motion, Elytra, Launchpad, cooldown, and anomaly observations; and
- an explicit statement that the approved values were for Client Test only and are not a
  production balance recommendation until Frontier decides after the test.
