# V0.0.1 Concrete Waymark Provider Result

- Status: `PAUSED — OWNER DECISION REQUIRED`
- Date: 2026-07-29
- Plugin source base: `16ce48dd0cdf905c5fcff5430b54459a72173e4a`
- Project authority source: `344eedc738d75954daa43facfeef302944f2963a`
- Decision record: `docs/adr/0007-fixed-rediseconomy-vault-provider-gate.md`

## Result matrix

| Area | Result | Evidence |
|---|---|---|
| Fixed RedisEconomy JAR | PASS | Version `4.5.12-wayfarer.1`; required SHA-256 matched |
| Fixed VaultUnlocked JAR | PASS | Version `2.20.2`; required SHA-256 matched |
| Source behavior / JAR bytecode | PASS | JAR contains the assessed cache-first, deferred Redis persistence path |
| Vault UUID-only API | BLOCKED | Vault accepts name or Bukkit `OfflinePlayer`, not UUID |
| Completion/failure contract | BLOCKED | Vault success precedes Redis completion; later failure is not returned |
| Atomic debit | BLOCKED | Fixed implementation uses separate cache read/check/write |
| Operation ID / effect lookup | BLOCKED | Not available on the fixed Vault/RedisEconomy surface |
| Concrete implementation | NOT RUN | Owner Gate B/C/D; intentionally not implemented |
| Concrete automated tests | NOT RUN | No approved implementation |
| Standalone runtime | NOT RUN | Would implicitly approve the blocked design |
| Candidate / JAR SHA | NOT CREATED | A blocked design cannot be a release candidate |
| Project Runtime | UNCHANGED | Read-only authority inspection only |

## Existing regression baseline

| Command | Result |
|---|---|
| `gradlew :plugins:wayfarer-core:test` with `BukkitWaymarkProviderSourceTest`, `TransactionEngineTest`, and `CoreRuntimeServicesTest` filters | PASS; 35 tests, 0 failed/errors/skipped |
| `gradlew check` | PASS; 174 unit, 14 MariaDB, and 6 Redis tests, 0 failed/errors/skipped |
| `gradlew assemble` | PASS |

The `check` integration tasks reused their valid Gradle cache entries; this documentation-only
branch did not change executable source, migration, or test inputs. Assemble emitted existing
Javadoc warnings only and completed successfully. No fixed external JAR, runtime config, world,
database, Redis instance, or Project permission was changed.

## Failed / skipped / N/A

There was no failed product test. Concrete-provider cases are `NOT RUN`, not PASS, because the
fixed API cannot satisfy the required safety contract without an Owner decision. Client interaction
is N/A. Candidate C and Client Fix Candidate history remain unchanged.

## Reproduction

Read-only reproduction requires the exact fixed JAR hashes and RedisEconomy source commit/blobs
listed in ADR 0007. Inspect:

- Vault `Economy` overloads;
- RedisEconomy `Currency.withdrawPlayer`, `depositPlayer`, `updateAccount`, and
  `updateAccountCloudCache`;
- `RedisManager.executeTransaction`;
- `CurrenciesManager.loadDefaultCurrency` and `terminate`;
- `RedisEconomyPlugin.onEnable` and `onDisable`.

Do not connect this reproduction to Project DB/Redis and do not infer a safe contract from a
successful balance command.
